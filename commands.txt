git status     check git status
git --version  check the version
git init      initilize a new repository 
git add .     add the files to the staging area.
git --help     this will show you all functions of git.
git commit -m "version 1"    this will commit the files changes. and save all.
git log        to check tha changes.
